{
  "name": "Symfony Customer-Process",
  "build": { "dockerfile": "Dockerfile" },
  "customizations": {
    "vscode": {
      "extensions": [
        "felixfbecker.php-intellisense",
        "bmewburn.vscode-intelephense-client",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker",
        "symfony.symfony-vscode"
      ]
    }
  },
  "forwardPorts": [8000],
  "postCreateCommand": "composer install && php bin/console doctrine:database:create --if-not-exists && php bin/console doctrine:migrations:migrate --no-interaction",
  "remoteUser": "vscode"
}
